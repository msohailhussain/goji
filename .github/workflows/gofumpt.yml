name: gofumpt

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  prettier:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@master
      with:
        set-safe-directory: '*'
        ref: ${{ github.head_ref }}
        fetch-depth: 0
    - name: auto-gofmt
      uses: iamnotaturtle/auto-gofmt@master
